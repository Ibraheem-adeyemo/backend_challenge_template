/**
 * The functions in this file is helping to extract the required data from data fetching from database and return the required data to their corresponding controllers.
 *  */

// const fromAllProductAttribute = (allAttribute) => {
//     allAttribute.map(attribute => {
//         const { }
//     })
// };

export default fromAllProductAttribute;
